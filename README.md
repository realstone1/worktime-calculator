# Worktime Calculator

HARU weekly work-time data helper for calculating PMS work minutes and overtime request times.

## Use

Open `index.html` in a browser, or publish this repository with GitHub Pages.

The app is a single static HTML file. It does not use a server, database, login, API, or saved browser storage.

